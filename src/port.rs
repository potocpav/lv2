/*
struct Port {
    dir: Direction,
    ty: Type,
}

enum Direction {
    Input,
    Output,
}

enum Type {
    Control(ControlSpec),
    Atom,
}

struct ControlSpec {
    default: f32,
    minimum: f32,
    maximum: f32,
}
*/
